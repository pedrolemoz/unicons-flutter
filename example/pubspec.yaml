name: unicons_example
description: An example for unicons package

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  unicons:

dev_dependencies:

flutter:
  uses-material-design: true