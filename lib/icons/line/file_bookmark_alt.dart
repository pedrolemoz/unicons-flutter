// Code generated using SVG Painter package
// Generated in 2024-08-26T20:39:57.907422

import 'dart:math' as math;

import 'package:flutter/material.dart';

class FileBookmarkAltIcon extends StatelessWidget {
  final Color? color;

  const FileBookmarkAltIcon({
    super.key,
    this.color,
  });

  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(
      builder: (context, constraints) {
        final colorScheme = Theme.of(context).colorScheme;
        final size = Size(constraints.maxWidth, constraints.maxHeight);

        return CustomPaint(
          size: size,
          painter: FileBookmarkAltPainter(color: color ?? colorScheme.primary),
        );
      },
    );
  }
}

class FileBookmarkAltPainter extends CustomPainter {
  final Color color;

  const FileBookmarkAltPainter({
    super.repaint,
    required this.color,
  });

  @override
  void paint(Canvas canvas, Size size) {
    final scaleX = size.width / 19.0;
    final scaleY = size.height / 20.043423948728968;
    final scale = math.min(scaleX, scaleY);

    final translationX = (size.width - 19.0 * scale) / 2 - 2.5 * scale;
    final translationY = (size.height - 20.043423948728968 * scale) / 2 - 2.0 * scale;

    final path = Path();
    final paint = Paint()..color = color;

    path.moveTo(
      7.5 * scale + translationX,
      10.0 * scale + translationY,
    );

    path.lineTo(
      8.5 * scale + translationX,
      10.0 * scale + translationY,
    );

    path.cubicTo(
      9.052284749830793 * scale + translationX,
      10.0 * scale + translationY,
      9.5 * scale + translationX,
      9.552284749830793 * scale + translationY,
      9.5 * scale + translationX,
      9.0 * scale + translationY,
    );

    path.cubicTo(
      9.5 * scale + translationX,
      8.447715250169207 * scale + translationY,
      9.052284749830793 * scale + translationX,
      8.0 * scale + translationY,
      8.5 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.lineTo(
      7.5 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.cubicTo(
      6.947715250169207 * scale + translationX,
      8.0 * scale + translationY,
      6.5 * scale + translationX,
      8.447715250169207 * scale + translationY,
      6.5 * scale + translationX,
      9.0 * scale + translationY,
    );

    path.cubicTo(
      6.5 * scale + translationX,
      9.552284749830793 * scale + translationY,
      6.947715250169207 * scale + translationX,
      10.0 * scale + translationY,
      7.5 * scale + translationX,
      10.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      11.5 * scale + translationX,
      16.0 * scale + translationY,
    );

    path.lineTo(
      7.5 * scale + translationX,
      16.0 * scale + translationY,
    );

    path.cubicTo(
      6.947715250169207 * scale + translationX,
      16.0 * scale + translationY,
      6.5 * scale + translationX,
      16.447715250169207 * scale + translationY,
      6.5 * scale + translationX,
      17.0 * scale + translationY,
    );

    path.cubicTo(
      6.5 * scale + translationX,
      17.552284749830793 * scale + translationY,
      6.947715250169207 * scale + translationX,
      18.0 * scale + translationY,
      7.5 * scale + translationX,
      18.0 * scale + translationY,
    );

    path.lineTo(
      11.5 * scale + translationX,
      18.0 * scale + translationY,
    );

    path.cubicTo(
      12.052284749830793 * scale + translationX,
      18.0 * scale + translationY,
      12.5 * scale + translationX,
      17.552284749830793 * scale + translationY,
      12.5 * scale + translationX,
      17.0 * scale + translationY,
    );

    path.cubicTo(
      12.5 * scale + translationX,
      16.447715250169207 * scale + translationY,
      12.052284749830793 * scale + translationX,
      16.0 * scale + translationY,
      11.5 * scale + translationX,
      16.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      11.5 * scale + translationX,
      12.0 * scale + translationY,
    );

    path.lineTo(
      7.5 * scale + translationX,
      12.0 * scale + translationY,
    );

    path.cubicTo(
      6.947715250169207 * scale + translationX,
      12.0 * scale + translationY,
      6.5 * scale + translationX,
      12.447715250169207 * scale + translationY,
      6.5 * scale + translationX,
      13.0 * scale + translationY,
    );

    path.cubicTo(
      6.5 * scale + translationX,
      13.552284749830793 * scale + translationY,
      6.947715250169207 * scale + translationX,
      14.0 * scale + translationY,
      7.5 * scale + translationX,
      14.0 * scale + translationY,
    );

    path.lineTo(
      11.5 * scale + translationX,
      14.0 * scale + translationY,
    );

    path.cubicTo(
      12.052284749830793 * scale + translationX,
      14.0 * scale + translationY,
      12.5 * scale + translationX,
      13.552284749830793 * scale + translationY,
      12.5 * scale + translationX,
      13.0 * scale + translationY,
    );

    path.cubicTo(
      12.5 * scale + translationX,
      12.447715250169207 * scale + translationY,
      12.052284749830793 * scale + translationX,
      12.0 * scale + translationY,
      11.5 * scale + translationX,
      12.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      18.42 * scale + translationX,
      9.379999999999999 * scale + translationY,
    );

    path.cubicTo(
      18.576596730083192 * scale + translationX,
      9.007602904624385 * scale + translationY,
      18.493744562147484 * scale + translationX,
      8.57756069962475 * scale + translationY,
      18.21 * scale + translationX,
      8.29 * scale + translationY,
    );

    path.lineTo(
      12.21 * scale + translationX,
      2.289999999999999 * scale + translationY,
    );

    path.lineTo(
      12.21 * scale + translationX,
      2.289999999999999 * scale + translationY,
    );

    path.cubicTo(
      12.127335666786882 * scale + translationX,
      2.2122162965759196 * scale + translationY,
      12.032820743132822 * scale + translationX,
      2.1480811698106663 * scale + translationY,
      11.930000492375418 * scale + translationX,
      2.1000000866712805 * scale + translationY,
    );

    path.cubicTo(
      11.900149534318432 * scale + translationX,
      2.095760137487866 * scale + translationY,
      11.86984993438067 * scale + translationX,
      2.095760137487866 * scale + translationY,
      11.839999735355379 * scale + translationX,
      2.0999999530613414 * scale + translationY,
    );

    path.lineTo(
      11.56 * scale + translationX,
      2.0 * scale + translationY,
    );

    path.lineTo(
      5.5 * scale + translationX,
      2.0 * scale + translationY,
    );

    path.cubicTo(
      3.8431458445892073 * scale + translationX,
      2.000000010226537 * scale + translationY,
      2.5000000251276986 * scale + translationX,
      3.343145829688045 * scale + translationY,
      2.500000074505806 * scale + translationX,
      5.000000149011611 * scale + translationY,
    );

    path.lineTo(
      2.5 * scale + translationX,
      19.0 * scale + translationY,
    );

    path.cubicTo(
      2.500000025127699 * scale + translationX,
      20.65685488556769 * scale + translationY,
      3.843145844589206 * scale + translationX,
      22.0000007050292 * scale + translationY,
      5.500000163912771 * scale + translationX,
      22.000000655651093 * scale + translationY,
    );

    path.lineTo(
      11.5 * scale + translationX,
      22.0 * scale + translationY,
    );

    path.cubicTo(
      12.052284749830793 * scale + translationX,
      22.0 * scale + translationY,
      12.5 * scale + translationX,
      21.552284749830793 * scale + translationY,
      12.5 * scale + translationX,
      21.0 * scale + translationY,
    );

    path.cubicTo(
      12.5 * scale + translationX,
      20.447715250169207 * scale + translationY,
      12.052284749830793 * scale + translationX,
      20.0 * scale + translationY,
      11.5 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      5.5 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.cubicTo(
      4.947715250169207 * scale + translationX,
      20.0 * scale + translationY,
      4.5 * scale + translationX,
      19.552284749830793 * scale + translationY,
      4.5 * scale + translationX,
      19.0 * scale + translationY,
    );

    path.lineTo(
      4.5 * scale + translationX,
      5.0 * scale + translationY,
    );

    path.cubicTo(
      4.5 * scale + translationX,
      4.447715250169207 * scale + translationY,
      4.947715250169207 * scale + translationX,
      4.0 * scale + translationY,
      5.5 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      10.5 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      10.5 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      10.500000263546278 * scale + translationX,
      8.65685452793982 * scale + translationY,
      11.843146083007786 * scale + translationX,
      10.000000347401329 * scale + translationY,
      13.500000402331352 * scale + translationX,
      10.000000298023224 * scale + translationY,
    );

    path.lineTo(
      17.5 * scale + translationX,
      10.0 * scale + translationY,
    );

    path.cubicTo(
      17.903701885711413 * scale + translationX,
      9.99798485482611 * scale + translationY,
      18.266596401679354 * scale + translationX,
      9.753425507108584 * scale + translationY,
      18.42 * scale + translationX,
      9.38 * scale + translationY,
    );

    path.close();

    path.moveTo(
      13.5 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.cubicTo(
      12.947715250169207 * scale + translationX,
      8.0 * scale + translationY,
      12.5 * scale + translationX,
      7.552284749830793 * scale + translationY,
      12.5 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.lineTo(
      12.5 * scale + translationX,
      5.41 * scale + translationY,
    );

    path.lineTo(
      15.09 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      20.5 * scale + translationX,
      12.0 * scale + translationY,
    );

    path.lineTo(
      15.5 * scale + translationX,
      12.0 * scale + translationY,
    );

    path.cubicTo(
      14.947715250169207 * scale + translationX,
      12.0 * scale + translationY,
      14.5 * scale + translationX,
      12.447715250169207 * scale + translationY,
      14.5 * scale + translationX,
      13.0 * scale + translationY,
    );

    path.lineTo(
      14.5 * scale + translationX,
      21.0 * scale + translationY,
    );

    path.cubicTo(
      14.50098352604491 * scale + translationX,
      21.368593740700117 * scale + translationY,
      14.704652827421677 * scale + translationX,
      21.706761637325688 * scale + translationY,
      15.030000000000001 * scale + translationX,
      21.879999999999995 * scale + translationY,
    );

    path.cubicTo(
      15.348276489860554 * scale + translationX,
      22.043423948728968 * scale + translationY,
      15.729612564824933 * scale + translationX,
      22.024357144980748 * scale + translationY,
      16.03 * scale + translationX,
      21.83 * scale + translationY,
    );

    path.lineTo(
      18.0 * scale + translationX,
      20.53 * scale + translationY,
    );

    path.lineTo(
      20.0 * scale + translationX,
      21.83 * scale + translationY,
    );

    path.cubicTo(
      20.30387451604742 * scale + translationX,
      22.005739747559723 * scale + translationY,
      20.677644972149004 * scale + translationX,
      22.009278425922332 * scale + translationY,
      20.984792358224762 * scale + translationX,
      21.839323538960414 * scale + translationY,
    );

    path.cubicTo(
      21.29193974430052 * scale + translationX,
      21.669368651998496 * scale + translationY,
      21.487482889660104 * scale + translationX,
      21.350809780827372 * scale + translationY,
      21.5 * scale + translationX,
      21.0 * scale + translationY,
    );

    path.lineTo(
      21.5 * scale + translationX,
      13.0 * scale + translationY,
    );

    path.cubicTo(
      21.5 * scale + translationX,
      12.447715250169207 * scale + translationY,
      21.052284749830793 * scale + translationX,
      12.0 * scale + translationY,
      20.5 * scale + translationX,
      12.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      19.5 * scale + translationX,
      19.12 * scale + translationY,
    );

    path.lineTo(
      18.56 * scale + translationX,
      18.490000000000002 * scale + translationY,
    );

    path.cubicTo(
      18.221684268837333 * scale + translationX,
      18.26132347611776 * scale + translationY,
      17.778315731162664 * scale + translationX,
      18.26132347611776 * scale + translationY,
      17.439999999999998 * scale + translationX,
      18.490000000000002 * scale + translationY,
    );

    path.lineTo(
      16.499999999999996 * scale + translationX,
      19.12 * scale + translationY,
    );

    path.lineTo(
      16.499999999999996 * scale + translationX,
      14.0 * scale + translationY,
    );

    path.lineTo(
      19.499999999999996 * scale + translationX,
      14.0 * scale + translationY,
    );

    path.close();

    canvas.drawPath(path, paint);
  }

  @override
  bool shouldRepaint(covariant CustomPainter oldDelegate) => false;
}