// Code generated using SVG Painter package
// Generated in 2024-08-26T20:40:00.745281

import 'dart:math' as math;

import 'package:flutter/material.dart';

class StoreAltIcon extends StatelessWidget {
  final Color? color;

  const StoreAltIcon({
    super.key,
    this.color,
  });

  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(
      builder: (context, constraints) {
        final colorScheme = Theme.of(context).colorScheme;
        final size = Size(constraints.maxWidth, constraints.maxHeight);

        return CustomPaint(
          size: size,
          painter: StoreAltPainter(color: color ?? colorScheme.primary),
        );
      },
    );
  }
}

class StoreAltPainter extends CustomPainter {
  final Color color;

  const StoreAltPainter({
    super.repaint,
    required this.color,
  });

  @override
  void paint(Canvas canvas, Size size) {
    final scaleX = size.width / 22.000000685453415;
    final scaleY = size.height / 20.0;
    final scale = math.min(scaleX, scaleY);

    final translationX = (size.width - 22.000000685453415 * scale) / 2 - 1.0 * scale;
    final translationY = (size.height - 20.0 * scale) / 2 - 2.0 * scale;

    final path = Path();
    final paint = Paint()..color = color;

    path.moveTo(
      22.0 * scale + translationX,
      2.0 * scale + translationY,
    );

    path.lineTo(
      2.0 * scale + translationX,
      2.0 * scale + translationY,
    );

    path.cubicTo(
      1.4477152501692068 * scale + translationX,
      2.0 * scale + translationY,
      1.0 * scale + translationX,
      2.4477152501692068 * scale + translationY,
      1.0 * scale + translationX,
      3.0 * scale + translationY,
    );

    path.lineTo(
      1.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      1.0035643689974663 * scale + translationX,
      8.26825517349613 * scale + translationY,
      1.8042713395650878 * scale + translationX,
      9.397252001996476 * scale + translationY,
      3.000000089406967 * scale + translationX,
      9.820000292658804 * scale + translationY,
    );

    path.lineTo(
      3.0 * scale + translationX,
      21.0 * scale + translationY,
    );

    path.cubicTo(
      3.0 * scale + translationX,
      21.552284749830793 * scale + translationY,
      3.4477152501692068 * scale + translationX,
      22.0 * scale + translationY,
      4.0 * scale + translationX,
      22.0 * scale + translationY,
    );

    path.lineTo(
      20.0 * scale + translationX,
      22.0 * scale + translationY,
    );

    path.cubicTo(
      20.552284749830793 * scale + translationX,
      22.0 * scale + translationY,
      21.0 * scale + translationX,
      21.552284749830793 * scale + translationY,
      21.0 * scale + translationX,
      21.0 * scale + translationY,
    );

    path.lineTo(
      21.0 * scale + translationX,
      9.82 * scale + translationY,
    );

    path.cubicTo(
      22.19572937569065 * scale + translationX,
      9.397252001996476 * scale + translationY,
      22.99643634625827 * scale + translationX,
      8.26825517349613 * scale + translationY,
      23.000000685453415 * scale + translationX,
      7.000000208616257 * scale + translationY,
    );

    path.lineTo(
      23.0 * scale + translationX,
      3.0 * scale + translationY,
    );

    path.cubicTo(
      23.0 * scale + translationX,
      2.4477152501692068 * scale + translationY,
      22.552284749830793 * scale + translationX,
      2.0 * scale + translationY,
      22.0 * scale + translationX,
      2.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      15.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      17.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      17.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      17.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      16.552284749830793 * scale + translationX,
      8.0 * scale + translationY,
      16.0 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.cubicTo(
      15.447715250169207 * scale + translationX,
      8.0 * scale + translationY,
      15.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      15.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      11.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      13.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      12.552284749830793 * scale + translationX,
      8.0 * scale + translationY,
      12.0 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.cubicTo(
      11.447715250169207 * scale + translationX,
      8.0 * scale + translationY,
      11.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      11.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      7.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      9.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      9.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      9.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      8.552284749830793 * scale + translationX,
      8.0 * scale + translationY,
      8.0 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.cubicTo(
      7.447715250169207 * scale + translationX,
      8.0 * scale + translationY,
      7.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      7.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      4.0 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.cubicTo(
      3.4477152501692068 * scale + translationX,
      8.0 * scale + translationY,
      3.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      3.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.lineTo(
      3.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      5.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      5.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      5.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      4.552284749830793 * scale + translationX,
      8.0 * scale + translationY,
      4.0 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      14.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      10.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      10.0 * scale + translationX,
      16.0 * scale + translationY,
    );

    path.cubicTo(
      10.0 * scale + translationX,
      14.895430500338414 * scale + translationY,
      10.895430500338414 * scale + translationX,
      14.0 * scale + translationY,
      12.0 * scale + translationX,
      14.0 * scale + translationY,
    );

    path.cubicTo(
      13.104569499661586 * scale + translationX,
      14.0 * scale + translationY,
      14.0 * scale + translationX,
      14.895430500338414 * scale + translationY,
      14.0 * scale + translationX,
      16.0 * scale + translationY,
    );

    path.close();

    path.moveTo(
      19.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      16.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      16.0 * scale + translationX,
      16.0 * scale + translationY,
    );

    path.cubicTo(
      16.0 * scale + translationX,
      13.790861000676827 * scale + translationY,
      14.209138999323173 * scale + translationX,
      12.0 * scale + translationY,
      12.0 * scale + translationX,
      12.0 * scale + translationY,
    );

    path.cubicTo(
      9.790861000676827 * scale + translationX,
      12.0 * scale + translationY,
      8.0 * scale + translationX,
      13.790861000676827 * scale + translationY,
      8.0 * scale + translationX,
      16.0 * scale + translationY,
    );

    path.lineTo(
      8.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      5.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      5.0 * scale + translationX,
      9.82 * scale + translationY,
    );

    path.cubicTo(
      5.367569917999487 * scale + translationX,
      9.683562399433667 * scale + translationY,
      5.7066398047683995 * scale + translationX,
      9.480120467372322 * scale + translationY,
      5.9999999620147255 * scale + translationX,
      9.219999941629297 * scale + translationY,
    );

    path.cubicTo(
      7.138802818862906 * scale + translationX,
      10.238576375860381 * scale + translationY,
      8.861197657974252 * scale + translationX,
      10.238576375860381 * scale + translationY,
      10.000000298023224 * scale + translationX,
      9.220000274777412 * scale + translationY,
    );

    path.cubicTo(
      11.138802938072196 * scale + translationX,
      10.238576375860381 * scale + translationY,
      12.861197777183541 * scale + translationX,
      10.238576375860381 * scale + translationY,
      14.000000417232513 * scale + translationX,
      9.220000274777412 * scale + translationY,
    );

    path.cubicTo(
      15.138803057281486 * scale + translationX,
      10.238576375860381 * scale + translationY,
      16.86119789639283 * scale + translationX,
      10.238576375860381 * scale + translationY,
      18.000000536441803 * scale + translationX,
      9.220000274777412 * scale + translationY,
    );

    path.cubicTo(
      18.293360043290505 * scale + translationX,
      9.48012046737232 * scale + translationY,
      18.63242993005942 * scale + translationX,
      9.683562399433667 * scale + translationY,
      18.9999998797133 * scale + translationX,
      9.819999937830767 * scale + translationY,
    );

    path.close();

    path.moveTo(
      21.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      21.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      20.552284749830793 * scale + translationX,
      8.0 * scale + translationY,
      20.0 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.cubicTo(
      19.447715250169207 * scale + translationX,
      8.0 * scale + translationY,
      19.0 * scale + translationX,
      7.552284749830793 * scale + translationY,
      19.0 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.lineTo(
      19.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.lineTo(
      21.0 * scale + translationX,
      4.0 * scale + translationY,
    );

    path.close();

    canvas.drawPath(path, paint);
  }

  @override
  bool shouldRepaint(covariant CustomPainter oldDelegate) => false;
}