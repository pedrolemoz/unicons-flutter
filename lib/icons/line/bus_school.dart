// Code generated using SVG Painter package
// Generated in 2024-08-26T20:39:56.288514

import 'dart:math' as math;

import 'package:flutter/material.dart';

class BusSchoolIcon extends StatelessWidget {
  final Color? color;

  const BusSchoolIcon({
    super.key,
    this.color,
  });

  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(
      builder: (context, constraints) {
        final colorScheme = Theme.of(context).colorScheme;
        final size = Size(constraints.maxWidth, constraints.maxHeight);

        return CustomPaint(
          size: size,
          painter: BusSchoolPainter(color: color ?? colorScheme.primary),
        );
      },
    );
  }
}

class BusSchoolPainter extends CustomPainter {
  final Color color;

  const BusSchoolPainter({
    super.repaint,
    required this.color,
  });

  @override
  void paint(Canvas canvas, Size size) {
    final scaleX = size.width / 22.000000734831524;
    final scaleY = size.height / 17.000000521540642;
    final scale = math.min(scaleX, scaleY);

    final translationX = (size.width - 22.000000734831524 * scale) / 2 - 1.0 * scale;
    final translationY = (size.height - 17.000000521540642 * scale) / 2 - 3.5 * scale;

    final path = Path();
    final paint = Paint()..color = color;

    path.moveTo(
      1.0 * scale + translationX,
      12.5 * scale + translationY,
    );

    path.lineTo(
      1.0 * scale + translationX,
      16.5 * scale + translationY,
    );

    path.cubicTo(
      1.0 * scale + translationX,
      17.052284749830793 * scale + translationY,
      1.4477152501692068 * scale + translationX,
      17.5 * scale + translationY,
      2.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.lineTo(
      3.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.cubicTo(
      3.0000001788139343 * scale + translationX,
      19.15685477103302 * scale + translationY,
      4.343145929321555 * scale + translationX,
      20.500000521540642 * scale + translationY,
      6.000000178813934 * scale + translationX,
      20.500000521540642 * scale + translationY,
    );

    path.cubicTo(
      7.656854428306314 * scale + translationX,
      20.500000521540642 * scale + translationY,
      9.000000178813934 * scale + translationX,
      19.15685477103302 * scale + translationY,
      9.000000178813934 * scale + translationX,
      17.500000521540642 * scale + translationY,
    );

    path.lineTo(
      15.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.cubicTo(
      15.000000536441803 * scale + translationX,
      19.15685477103302 * scale + translationY,
      16.343146286949423 * scale + translationX,
      20.500000521540642 * scale + translationY,
      18.000000536441803 * scale + translationX,
      20.500000521540642 * scale + translationY,
    );

    path.cubicTo(
      19.656854785934183 * scale + translationX,
      20.500000521540642 * scale + translationY,
      21.000000536441803 * scale + translationX,
      19.15685477103302 * scale + translationY,
      21.000000536441803 * scale + translationX,
      17.500000521540642 * scale + translationY,
    );

    path.lineTo(
      22.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.cubicTo(
      22.552284749830793 * scale + translationX,
      17.5 * scale + translationY,
      23.0 * scale + translationX,
      17.052284749830793 * scale + translationY,
      23.0 * scale + translationX,
      16.5 * scale + translationY,
    );

    path.lineTo(
      23.0 * scale + translationX,
      6.5 * scale + translationY,
    );

    path.cubicTo(
      23.000000734831524 * scale + translationX,
      4.8431458743915305 * scale + translationY,
      21.656854915370012 * scale + translationX,
      3.5000000549300223 * scale + translationY,
      20.000000596046448 * scale + translationX,
      3.5000001043081284 * scale + translationY,
    );

    path.lineTo(
      8.44 * scale + translationX,
      3.5 * scale + translationY,
    );

    path.cubicTo(
      7.15128537786943 * scale + translationX,
      3.502411011994798 * scale + translationY,
      6.008091642960848 * scale + translationX,
      4.327603672756274 * scale + translationY,
      5.600000166893006 * scale + translationX,
      5.5500001654028885 * scale + translationY,
    );

    path.lineTo(
      4.16 * scale + translationX,
      9.86 * scale + translationY,
    );

    path.lineTo(
      1.45 * scale + translationX,
      11.67 * scale + translationY,
    );

    path.cubicTo(
      1.1706147877820652 * scale + translationX,
      11.853983488299214 * scale + translationY,
      1.0017306467766538 * scale + translationX,
      12.165480903931417 * scale + translationY,
      1.0000000000000002 * scale + translationX,
      12.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      21.0 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.lineTo(
      19.0 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.lineTo(
      19.0 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.lineTo(
      20.0 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.cubicTo(
      20.552284749830793 * scale + translationX,
      5.5 * scale + translationY,
      21.0 * scale + translationX,
      5.947715250169207 * scale + translationY,
      21.0 * scale + translationX,
      6.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      17.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.cubicTo(
      17.0 * scale + translationX,
      16.947715250169207 * scale + translationY,
      17.447715250169207 * scale + translationX,
      16.5 * scale + translationY,
      18.0 * scale + translationX,
      16.5 * scale + translationY,
    );

    path.cubicTo(
      18.552284749830793 * scale + translationX,
      16.5 * scale + translationY,
      19.0 * scale + translationX,
      16.947715250169207 * scale + translationY,
      19.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.cubicTo(
      19.0 * scale + translationX,
      18.052284749830793 * scale + translationY,
      18.552284749830793 * scale + translationX,
      18.5 * scale + translationY,
      18.0 * scale + translationX,
      18.5 * scale + translationY,
    );

    path.cubicTo(
      17.447715250169207 * scale + translationX,
      18.5 * scale + translationY,
      17.0 * scale + translationX,
      18.052284749830793 * scale + translationY,
      17.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      15.0 * scale + translationX,
      11.5 * scale + translationY,
    );

    path.lineTo(
      21.0 * scale + translationX,
      11.5 * scale + translationY,
    );

    path.lineTo(
      21.0 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.lineTo(
      20.22 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.cubicTo(
      19.651427186841175 * scale + translationX,
      14.874457704528059 * scale + translationY,
      18.845328561800333 * scale + translationX,
      14.517820995641722 * scale + translationY,
      18.000000536441803 * scale + translationX,
      14.517820995641722 * scale + translationY,
    );

    path.cubicTo(
      17.154672511083273 * scale + translationX,
      14.517820995641722 * scale + translationY,
      16.34857388604243 * scale + translationX,
      14.874457704528059 * scale + translationY,
      15.780000470280646 * scale + translationX,
      15.500000461935997 * scale + translationY,
    );

    path.lineTo(
      15.0 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      15.0 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.lineTo(
      17.0 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.lineTo(
      17.0 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.lineTo(
      15.0 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      11.0 * scale + translationX,
      11.5 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      11.5 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.lineTo(
      11.0 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      11.0 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.lineTo(
      11.0 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      9.0 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.lineTo(
      6.39 * scale + translationX,
      9.5 * scale + translationY,
    );

    path.lineTo(
      7.49 * scale + translationX,
      6.18 * scale + translationY,
    );

    path.cubicTo(
      7.627572117790846 * scale + translationX,
      5.772688282525956 * scale + translationY,
      8.01008408819971 * scale + translationX,
      5.498890240549085 * scale + translationY,
      8.44 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.lineTo(
      9.0 * scale + translationX,
      5.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      5.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.cubicTo(
      5.0 * scale + translationX,
      16.947715250169207 * scale + translationY,
      5.447715250169207 * scale + translationX,
      16.5 * scale + translationY,
      6.0 * scale + translationX,
      16.5 * scale + translationY,
    );

    path.cubicTo(
      6.552284749830793 * scale + translationX,
      16.5 * scale + translationY,
      7.0 * scale + translationX,
      16.947715250169207 * scale + translationY,
      7.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.cubicTo(
      7.0 * scale + translationX,
      18.052284749830793 * scale + translationY,
      6.552284749830793 * scale + translationX,
      18.5 * scale + translationY,
      6.0 * scale + translationX,
      18.5 * scale + translationY,
    );

    path.cubicTo(
      5.447715250169207 * scale + translationX,
      18.5 * scale + translationY,
      5.0 * scale + translationX,
      18.052284749830793 * scale + translationY,
      5.0 * scale + translationX,
      17.5 * scale + translationY,
    );

    path.close();

    path.moveTo(
      3.0 * scale + translationX,
      13.0 * scale + translationY,
    );

    path.lineTo(
      5.3 * scale + translationX,
      11.5 * scale + translationY,
    );

    path.lineTo(
      9.0 * scale + translationX,
      11.5 * scale + translationY,
    );

    path.lineTo(
      9.0 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.lineTo(
      8.22 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.cubicTo(
      7.651426829213305 * scale + translationX,
      14.87445770452806 * scale + translationY,
      6.845328204172464 * scale + translationX,
      14.517820995641724 * scale + translationY,
      6.000000178813934 * scale + translationX,
      14.517820995641724 * scale + translationY,
    );

    path.cubicTo(
      5.154672153455405 * scale + translationX,
      14.517820995641724 * scale + translationY,
      4.348573528414565 * scale + translationX,
      14.87445770452806 * scale + translationY,
      3.780000112652779 * scale + translationX,
      15.500000461935997 * scale + translationY,
    );

    path.lineTo(
      3.0 * scale + translationX,
      15.5 * scale + translationY,
    );

    path.close();

    canvas.drawPath(path, paint);
  }

  @override
  bool shouldRepaint(covariant CustomPainter oldDelegate) => false;
}