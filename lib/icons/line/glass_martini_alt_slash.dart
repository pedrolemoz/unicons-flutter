// Code generated using SVG Painter package
// Generated in 2024-08-26T20:39:58.343796

import 'dart:math' as math;

import 'package:flutter/material.dart';

class GlassMartiniAltSlashIcon extends StatelessWidget {
  final Color? color;

  const GlassMartiniAltSlashIcon({
    super.key,
    this.color,
  });

  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(
      builder: (context, constraints) {
        final colorScheme = Theme.of(context).colorScheme;
        final size = Size(constraints.maxWidth, constraints.maxHeight);

        return CustomPaint(
          size: size,
          painter: GlassMartiniAltSlashPainter(color: color ?? colorScheme.primary),
        );
      },
    );
  }
}

class GlassMartiniAltSlashPainter extends CustomPainter {
  final Color color;

  const GlassMartiniAltSlashPainter({
    super.repaint,
    required this.color,
  });

  @override
  void paint(Canvas canvas, Size size) {
    final scaleX = size.width / 16.145157031219117;
    final scaleY = size.height / 20.102122113990337;
    final scale = math.min(scaleX, scaleY);

    final translationX = (size.width - 16.145157031219117 * scale) / 2 - 3.897877918467569 * scale;
    final translationY = (size.height - 20.102122113990337 * scale) / 2 - 1.8978778860096637 * scale;

    final path = Path();
    final paint = Paint()..color = color;

    path.moveTo(
      19.71 * scale + translationX,
      16.29 * scale + translationY,
    );

    path.lineTo(
      5.710000000000001 * scale + translationX,
      2.289999999999999 * scale + translationY,
    );

    path.cubicTo(
      5.317877905966421 * scale + translationX,
      1.8978778860096637 * scale + translationY,
      4.682122256323103 * scale + translationX,
      1.8978778860096637 * scale + translationY,
      4.2900000873953354 * scale + translationX,
      2.2900000549374306 * scale + translationY,
    );

    path.cubicTo(
      3.897877918467569 * scale + translationX,
      2.682122223865197 * scale + translationY,
      3.8978779184675694 * scale + translationX,
      3.3178778735085164 * scale + translationY,
      4.290000087395336 * scale + translationX,
      3.710000042436283 * scale + translationY,
    );

    path.lineTo(
      6.59 * scale + translationX,
      6.0 * scale + translationY,
    );

    path.lineTo(
      5.0 * scale + translationX,
      6.0 * scale + translationY,
    );

    path.cubicTo(
      4.6153405567375945 * scale + translationX,
      6.0010888966801295 * scale + translationY,
      4.265402682104507 * scale + translationX,
      6.222716217281085 * scale + translationY,
      4.1 * scale + translationX,
      6.57 * scale + translationY,
    );

    path.cubicTo(
      3.956965050313318 * scale + translationX,
      6.900616041050955 * scale + translationY,
      4.0028045186677605 * scale + translationX,
      7.282611610671317 * scale + translationY,
      4.22 * scale + translationX,
      7.57 * scale + translationY,
    );

    path.lineTo(
      11.0 * scale + translationX,
      16.1 * scale + translationY,
    );

    path.lineTo(
      11.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      6.75 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.cubicTo(
      6.197715250169207 * scale + translationX,
      20.0 * scale + translationY,
      5.75 * scale + translationX,
      20.447715250169207 * scale + translationY,
      5.75 * scale + translationX,
      21.0 * scale + translationY,
    );

    path.cubicTo(
      5.75 * scale + translationX,
      21.552284749830793 * scale + translationY,
      6.197715250169207 * scale + translationX,
      22.0 * scale + translationY,
      6.75 * scale + translationX,
      22.0 * scale + translationY,
    );

    path.lineTo(
      17.25 * scale + translationX,
      22.0 * scale + translationY,
    );

    path.cubicTo(
      17.802284749830793 * scale + translationX,
      22.0 * scale + translationY,
      18.25 * scale + translationX,
      21.552284749830793 * scale + translationY,
      18.25 * scale + translationX,
      21.0 * scale + translationY,
    );

    path.cubicTo(
      18.25 * scale + translationX,
      20.447715250169207 * scale + translationY,
      17.802284749830793 * scale + translationX,
      20.0 * scale + translationY,
      17.25 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      20.0 * scale + translationY,
    );

    path.lineTo(
      13.0 * scale + translationX,
      16.1 * scale + translationY,
    );

    path.lineTo(
      14.64 * scale + translationX,
      14.100000000000001 * scale + translationY,
    );

    path.lineTo(
      18.29 * scale + translationX,
      17.75 * scale + translationY,
    );

    path.cubicTo(
      18.477766599905554 * scale + translationX,
      17.939312650823098 * scale + translationY,
      18.73336246362944 * scale + translationX,
      18.04579832434167 * scale + translationY,
      19.0 * scale + translationX,
      18.04579832434167 * scale + translationY,
    );

    path.cubicTo(
      19.26663753637056 * scale + translationX,
      18.04579832434167 * scale + translationY,
      19.522233400094446 * scale + translationX,
      17.939312650823098 * scale + translationY,
      19.71 * scale + translationX,
      17.75 * scale + translationY,
    );

    path.cubicTo(
      19.911950321431444 * scale + translationX,
      17.56092825943442 * scale + translationY,
      20.02655285500633 * scale + translationX,
      17.296644275933485 * scale + translationY,
      20.02655285500633 * scale + translationX,
      17.02 * scale + translationY,
    );

    path.cubicTo(
      20.02655285500633 * scale + translationX,
      16.743355724066515 * scale + translationY,
      19.911950321431444 * scale + translationX,
      16.47907174056558 * scale + translationY,
      19.71 * scale + translationX,
      16.29 * scale + translationY,
    );

    path.close();

    path.moveTo(
      7.08 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.lineTo(
      8.59 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.lineTo(
      10.48 * scale + translationX,
      9.89 * scale + translationY,
    );

    path.lineTo(
      8.59 * scale + translationX,
      9.89 * scale + translationY,
    );

    path.close();

    path.moveTo(
      12.0 * scale + translationX,
      14.15 * scale + translationY,
    );

    path.lineTo(
      10.19 * scale + translationX,
      11.89 * scale + translationY,
    );

    path.lineTo(
      12.48 * scale + translationX,
      11.89 * scale + translationY,
    );

    path.lineTo(
      13.22 * scale + translationX,
      12.63 * scale + translationY,
    );

    path.close();

    path.moveTo(
      14.66 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.lineTo(
      16.92 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.lineTo(
      16.290000000000003 * scale + translationX,
      8.79 * scale + translationY,
    );

    path.cubicTo(
      15.947317594450276 * scale + translationX,
      9.21875146834145 * scale + translationY,
      16.01426341616343 * scale + translationX,
      9.843579137664221 * scale + translationY,
      16.44 * scale + translationX,
      10.189999999999998 * scale + translationY,
    );

    path.cubicTo(
      16.61861585258866 * scale + translationX,
      10.333304337827025 * scale + translationY,
      16.84100474495217 * scale + translationX,
      10.410963951033331 * scale + translationY,
      17.07 * scale + translationX,
      10.410000000000002 * scale + translationY,
    );

    path.cubicTo(
      17.372588411990066 * scale + translationX,
      10.411028860405116 * scale + translationY,
      17.659369947635206 * scale + translationX,
      10.274991465291397 * scale + translationY,
      17.85 * scale + translationX,
      10.040000000000001 * scale + translationY,
    );

    path.lineTo(
      19.78 * scale + translationX,
      7.620000000000001 * scale + translationY,
    );

    path.cubicTo(
      19.99719548133224 * scale + translationX,
      7.332611610671318 * scale + translationY,
      20.043034949686685 * scale + translationX,
      6.950616041050955 * scale + translationY,
      19.900000000000002 * scale + translationX,
      6.62 * scale + translationY,
    );

    path.cubicTo(
      19.749450232262916 * scale + translationX,
      6.253179690162848 * scale + translationY,
      19.396387681620197 * scale + translationX,
      6.009958821942311 * scale + translationY,
      19.0 * scale + translationX,
      6.0 * scale + translationY,
    );

    path.lineTo(
      14.66 * scale + translationX,
      6.0 * scale + translationY,
    );

    path.cubicTo(
      14.107715250169207 * scale + translationX,
      6.0 * scale + translationY,
      13.66 * scale + translationX,
      6.447715250169207 * scale + translationY,
      13.66 * scale + translationX,
      7.0 * scale + translationY,
    );

    path.cubicTo(
      13.66 * scale + translationX,
      7.552284749830793 * scale + translationY,
      14.107715250169207 * scale + translationX,
      8.0 * scale + translationY,
      14.66 * scale + translationX,
      8.0 * scale + translationY,
    );

    path.close();

    canvas.drawPath(path, paint);
  }

  @override
  bool shouldRepaint(covariant CustomPainter oldDelegate) => false;
}